import { Suspense } from "react";
import { fetchPromptsList } from "../lib/services/request-service/prompts-request-service";
import EnhancedTable from "./data-table";

export default async function PromptsTable() {
    /*const fetchPrompts = await fetchPromptsList();
    return (
        <EnhancedTable list={retrievePrompts(fetchPrompts)} />
    )*/
}