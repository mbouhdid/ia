import Header from "../ui/header";


export default async function Page({ params }: { params: { id: string } }) {

    return (
        <>

            <Header title="List prompts template" />

        </>
    );
}